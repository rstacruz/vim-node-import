# vim-node-import

```js
fs<ctrl-e>
```

turns into:

```js
const fs = require('fs')
```

also:

```js
path          → const path = require('path')
../setup      → const setup = require('../setup')
path.join     → const join = require('path').join
```
